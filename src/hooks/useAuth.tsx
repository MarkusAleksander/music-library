/**
 * * Hook for managing and viewing the current Auth state
 */
const useAuth = () => {
  const login = () => {};

  const logout = () => {};

  return {
    login,
    logout,
  };
};

export default useAuth;
